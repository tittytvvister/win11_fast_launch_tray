# Win11 Fast Launch Tray

Лёгкий лаунчер для Windows 11: иконка возле часов открывает категоризированное меню ярлыков из обычной папки.

## Возможности

- запуск `.lnk` и `.url` из системного трея;
- категории из подпапок и автоматические правила для ярлыков в корне;
- автоматическая светлая/тёмная тема Windows 11;
- PerMonitorV2 DPI awareness и чёткий текст при масштабировании;
- единый размер иконок приложений без shortcut-overlay;
- кастомная tray-иконка;
- закрытие меню кликом вне него;
- автозапуск без прав администратора.

## Быстрый старт

1. Скачайте репозиторий или папку `dist`.
2. Создайте `%USERPROFILE%\Desktop\menu`.
3. Положите туда ярлыки программ и игр.
4. Запустите `dist\Install-TrayMenu.cmd`.
5. Если иконка скрыта, раскройте `^` возле часов и перетащите её на панель.

Лаунчер перечитывает папку при каждом открытии меню. Перезапуск после добавления ярлыка не нужен.

## Категории

Подпапки имеют приоритет и становятся категориями автоматически:

```text
menu/
  Игры/
    Cyberpunk 2077.lnk
  Работа/
    Recordly.lnk
  VR/
    SteamVR.url
```

Ярлыки в корне классифицируются по `rules` из `desktop-menu-config.json`. Неизвестные приложения попадают в `defaultCategory`.

## Конфигурация

Основные поля `config/desktop-menu-config.json`:

- `sourcePath` — папка ярлыков; поддерживаются переменные окружения, например `%USERPROFILE%`;
- `trayIconPath` — относительный или абсолютный путь к `.ico`;
- `categoryOrder` — порядок категорий;
- `rules` — регулярные выражения для классификации;
- `defaultCategory` — категория по умолчанию.

После установки рабочая копия находится в `%LOCALAPPDATA%\DesktopMenuLauncher`. Изменения исходного конфига попадают туда при повторном запуске установщика.

## Управление

- повторный запуск после выхода: `dist\Start-TrayMenu.cmd` или `My Programs` в меню «Пуск»;
- обновление сборки: `dist\Install-TrayMenu.cmd`;
- полное удаление: `dist\Uninstall-TrayMenu.cmd`.

Удаление не затрагивает папку с пользовательскими ярлыками.

## Сборка

Требуется Windows с установленным .NET Framework 4.x:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\build.ps1
```

Скрипт компилирует приложение через системный `csc.exe`, генерирует иконку и пересобирает `dist`.

## Структура

```text
src/       исходный код и DPI manifest
tools/     генератор tray-иконки
config/    исходная конфигурация
scripts/   установка, запуск и удаление
dist/      готовая переносимая сборка
```

Расширенный пользовательский гайд: [Google Docs](https://docs.google.com/document/d/1waE8aHkH8kSrUYyWtYEP0nF3UxuUaoh2tXESrLzlyrk/edit).
